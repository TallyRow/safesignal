{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", ":dependencyDashboard"],
  "labels": ["dependencies"],
  "rebaseWhen": "behind-base-branch",
  "lockFileMaintenance": { "enabled": true },
  "packageRules": [
    {
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
      "groupName": "minor & patch updates"
    },
    {
      "description": "Isolate the @opentelemetry/* seam deps: they use 0.x versioning, so Renovate treats their (functionally breaking) bumps as 'minor' — keep them out of the grouped minor/patch MR and review deliberately.",
      "matchPackageNames": ["@opentelemetry/**"],
      "groupName": "opentelemetry",
      "separateMinorPatch": true
    }
  ]
}
